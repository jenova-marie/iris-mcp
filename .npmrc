enable-pre-post-scripts=true

# Disable git branch checks for publishing
# Allows publishing from detached HEAD state (e.g., GitHub Actions tag-based releases)
git-checks=false

# Allow publishing from any branch (not just master/main)
publish-branch=*
